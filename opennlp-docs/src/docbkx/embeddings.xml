<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE chapter PUBLIC "-//OASIS//DTD DocBook XML 5.0//EN"
"http://docbook.org/xml/5.0/dtd/docbook.dtd"[
]>
<!-- Licensed to the Apache Software Foundation (ASF) under one or more contributor
	license agreements. See the NOTICE file distributed with this work for additional
	information regarding copyright ownership. The ASF licenses this file to
	you under the Apache License, Version 2.0 (the "License"); you may not use
	this file except in compliance with the License. You may obtain a copy of
	the License at http://www.apache.org/licenses/LICENSE-2.0 Unless required
	by applicable law or agreed to in writing, software distributed under the
	License is distributed on an "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS
	OF ANY KIND, either express or implied. See the License for the specific
	language governing permissions and limitations under the License. -->

<chapter xml:id="tools.embeddings" xmlns:xlink="http://www.w3.org/1999/xlink">

	<title>Static Embeddings</title>

	<section xml:id="tools.embeddings.introduction">
		<title>Introduction</title>
		<para>
			The <code>opennlp-embeddings</code> extension module produces sentence and word
			embedding vectors from a static (non-contextual) embedding table: a per-token vector
			matrix plus subword tokenization, WordPiece or SentencePiece. It is the modern
			successor to the word2vec and GloVe workflow. Distillation tools can compress a
			sentence-transformer into such a flat table (the Model2Vec family of releases is the
			primary target), and looking a sentence up in the table approximates the transformer's
			semantics at a small fraction of the cost: embedding a text is tokenize, gather,
			mean-pool, and normalize, with no model forward pass, no GPU, and no native runtime.
			SentencePiece support covers multilingual tables distilled from encoders of the
			XLM-RoBERTa family, whose vectors embed different languages into the same space.
		</para>
		<para>
			Use it when embedding throughput and a pure-JVM deployment matter more than the last
			few points of retrieval quality: semantic similarity and deduplication, candidate
			retrieval for a heavier reranker, clustering, or classification features. A
			contextual model remains the better choice when distinguishing word senses in context
			is the point of the task.
		</para>
		<para>
			OpenNLP supports contextual, ONNX-backed sentence vectors as well, in the
			<code>opennlp-dl</code> module. Those are inherently more accurate; Model2Vec trades
			some accuracy for a large speed gain. Both paths implement the same
			<code>TextEmbedder</code> interface (in <code>opennlp-api</code>), so an application can
			swap one for the other without changing its calling code.
		</para>
		<para>
			No model is bundled with the module. Callers point it at a model directory they
			downloaded; the table's own license applies to the table.
		</para>
		<para>
			The public API of this module
			(<code>StaticEmbeddingModel</code>, <code>SafetensorsFile</code>,
			<code>TensorInfo</code>, <code>Neighbor</code>, <code>ModelDistiller</code>, and
			<code>ModelAssembler</code>), together with the <code>TextEmbedder</code> interface
			in <code>opennlp-api</code>, is experimental and may change in a later release.
		</para>
	</section>

	<section xml:id="tools.embeddings.api">
		<title>Embedding Text with the API</title>
		<para>
			A model directory loads with a single call, and the tokenizer family is detected from
			the files present. A WordPiece model carries <code>vocab.txt</code>,
			<code>model.safetensors</code>, <code>config.json</code>, and
			<code>tokenizer_config.json</code>; a SentencePiece model carries a trained
			<code>.model</code> file (<code>sentencepiece.bpe.model</code>,
			<code>spiece.model</code>, or <code>tokenizer.model</code>) next to
			<code>tokenizer.json</code>, <code>model.safetensors</code>, and
			<code>config.json</code>. The tokenizer and pooling switches are read from the model's
			own configuration files:
		</para>
		<programlisting language="java">
			<![CDATA[
StaticEmbeddingModel model = StaticEmbeddingModel.load(Path.of("/path/to/model-directory"));

float[] vector = model.embed("The quick brown fox");
double similarity = model.similarity("coffee", "espresso");
List<Neighbor> neighbors = model.mostSimilar("coffee", 5);
List<Neighbor> analogy = model.analogy("man", "king", "woman", 1);]]>
		</programlisting>
		<para>
			<code>StaticEmbeddingUsageExampleTest</code> asserts the load-and-query workflow
			shown here.
		</para>
		<para>
			For a model laid out differently, the explicit overloads take the data files and the
			switches directly. The WordPiece overload takes whether the tokenizer lower-cases
			(and strips accents) and whether embeddings are L2-normalized; both are properties of
			the model, published in its configuration. The SentencePiece overload has no casing
			switch, because the trained <code>.model</code> file carries the model's own text
			normalizer.
		</para>
		<programlisting language="java">
			<![CDATA[
StaticEmbeddingModel model = StaticEmbeddingModel.load(
    Path.of("vocab.txt"), Path.of("model.safetensors"),
    StaticEmbeddingModel.Casing.UNCASED,      // from the model's do_lower_case
    StaticEmbeddingModel.Normalization.L2);   // from the model's config

StaticEmbeddingModel multilingual = StaticEmbeddingModel.loadSentencePiece(
    Path.of("sentencepiece.bpe.model"), Path.of("tokenizer.json"),
    Path.of("model.safetensors"),
    StaticEmbeddingModel.Normalization.L2);
]]>
		</programlisting>
		<para>
			The <code>testExplicitOverloads</code> case of
			<code>StaticEmbeddingUsageExampleTest</code> asserts the two overloads shown here.
		</para>
		<para>
			Matrix rows are resolved by piece string, never by tokenizer id, because the two
			files of a SentencePiece model routinely order and offset their ids differently. A
			poolable piece with no matrix row fails loud at load time. Distillation output
			usually ships without the trained <code>.model</code> file; copy that one file from
			the teacher model's own repository into the model directory, and the loader names
			exactly this fix if the file is missing.
		</para>
		<para>
			Instances are immutable and safe for concurrent use, so one loaded model can serve
			every thread of an application. Texts with no in-vocabulary tokens embed to a zero
			vector rather than raising an error (matching the reference implementation), and
			<code>similarity</code> reports <code>0</code> for them. This is a rare edge case for
			text in the model's language, since subword tokenization backs off to smaller pieces;
			it mostly happens for empty input or text outside the vocabulary's coverage.
		</para>
	</section>

	<section xml:id="tools.embeddings.search">
		<title>Semantic Search</title>
		<para>
			The headline use case is ranking documents against a query by meaning rather than by
			shared words. <code>similarity</code> embeds both texts and returns the cosine
			similarity of their vectors, so a small document list ranks with one call per
			document:
		</para>
		<programlisting language="java">
			<![CDATA[
StaticEmbeddingModel model = StaticEmbeddingModel.load(Path.of("/path/to/model-directory"));

String query = "home espresso machine";
List<String> documents = List.of(
    "How do I brew espresso at home?",
    "The history of tea in East Asia",
    "Best grinders for pour-over coffee");

record Scored(String document, double score) {}
List<Scored> results = new ArrayList<>();
for (String document : documents) {
  results.add(new Scored(document, model.similarity(query, document)));
}
results.sort(Comparator.comparingDouble(Scored::score).reversed());]]>
		</programlisting>
		<para>
			<code>StaticEmbeddingSearchExampleTest</code> asserts the ranking shown here. For a
			corpus too large to score per query, embed each document once with
			<code>embed</code>, keep the vectors in any vector index, and embed only the query
			at search time; the index does not care how the vectors were produced.
		</para>
	</section>

	<section xml:id="tools.embeddings.cmdline">
		<title>Command Line Tools</title>
		<para>
			The module ships its own command line launcher, <code>bin/embeddings</code>, next to
			<code>bin/opennlp</code> in the binary distribution. Invoked without arguments it lists
			the available tools, and every tool prints its help when invoked with the
			<code>help</code> parameter.
		</para>
		<section xml:id="tools.embeddings.cmdline.distill">
			<title>Distill Model Tool</title>
			<para>
				The <code>DistillModel</code> tool compresses a sentence-transformer teacher into a
				static embedding table. The teacher is either a Hugging Face model id
				(<code>org/model</code>, or <code>org/model@revision</code> to pin a branch, tag, or
				commit; the required files download once into a local cache) or a local directory
				holding the teacher's <code>tokenizer.json</code> and <code>onnx/model.onnx</code>.
				The following command distills a teacher into the directory given by
				<code>-out</code>:
				<screen>
<![CDATA[$ bin/embeddings DistillModel -teacher hf-id-or-path -out dir [-pcaDims num] [-terms file]]]>
				</screen>
				<code>-pcaDims</code> is the number of principal components to keep and defaults
				to 256. The run ends by assembling the output directory and verifying it with
				<code>StaticEmbeddingModel.load</code>, then prints a summary naming the tokenizer
				family, the row count, the dimension reduction, and the variance the PCA kept, so a
				run that prints a summary is a directory that works.
			</para>
			<para>
				<code>-terms</code> names an optional term file: one term per line, with text after
				a tab ignored, so a learned vocabulary TSV works unchanged. Each term, a whole word
				or a multi-word phrase such as a domain vocabulary entry, is segmented by the
				teacher's own tokenizer, encoded through the teacher as one sequence, and appended
				to the table as an extra row, recorded in the model directory as
				<code>terms.txt</code>. When such a model embeds text, it first matches the text
				against its terms greedily longest-first, case-insensitively, and pools a matched
				term's single row instead of the subword pieces of its words; text between matches
				is tokenized as usual, and a model without a term file embeds exactly as before.
				Terms should arrive sorted by descending corpus frequency, because the Zipf
				weighting treats the subword rows and the term rows as one frequency ranking. A
				term that equals a vocabulary token is dropped as a duplicate row, and terms are
				also returned by the similarity search of <code>mostSimilar</code> like any
				vocabulary token.
			</para>
		</section>
		<section xml:id="tools.embeddings.cmdline.assemble">
			<title>Assemble Model Tool</title>
			<para>
				The <code>AssembleModel</code> tool completes a downloaded distillation in place so
				<code>StaticEmbeddingModel.load</code> can open it. A Model2Vec distillation writes
				<code>model.safetensors</code>, <code>tokenizer.json</code>, and
				<code>config.json</code>; for a WordPiece model the tool derives the missing
				<code>vocab.txt</code> and <code>tokenizer_config.json</code> from
				<code>tokenizer.json</code>, and for a SentencePiece model it checks that the
				trained <code>.model</code> file copied from the teacher is present, naming the fix
				if it is not. It never overwrites an existing file.
				<screen>
<![CDATA[$ bin/embeddings AssembleModel -modelDir dir]]>
				</screen>
				The tool then verifies the directory by loading it and prints the loaded model's
				family, row count, and dimension, along with a line for every file it wrote.
			</para>
		</section>
	</section>

	<section xml:id="tools.embeddings.quantized">
		<title>Quantized Models</title>
		<para>
			A static embedding table can be quantized to 2, 3, or 4 bits per dimension, shrinking
			it by roughly 4 to 16 times against the 32-bit float matrix, depending on the bit
			width and on how far the dimension is from a power of two: rows are padded to the
			next power of two before coding, so a 500,000-row, 300-dimension table stores 512
			coded dimensions per row and drops from about 600 MB to about 132 MB at 4 bits, 4.5
			times smaller. Because embedding is
			memory-bound row gathering, reading fewer bytes is also the throughput lever. The
			method is the TurboQuant construction: each row is rotated so its coordinates become
			near-independent and near-Gaussian, and each rotated coordinate is encoded against an
			optimal scalar grid, with a least-squares scale fitted per row.
		</para>
		<para>
			The <code>QuantizeModel</code> tool quantizes a model directory's
			<code>model.safetensors</code> in place, writing <code>model.quantized</code> next to
			it and reporting the sizes and the reconstruction quality it measured from the written
			file:
		</para>
		<programlisting language="text">
			<![CDATA[
opennlp-embeddings QuantizeModel -modelDir /path/to/model-directory -bits 4]]>
		</programlisting>
		<para>
			A directory presents exactly one matrix file. After quantizing, delete the
			<code>model.safetensors</code> to deploy the quantized matrix; the loader then reads
			<code>model.quantized</code> with no code change, and any per-token pooling weights
			are carried inside the quantized file. A directory that still holds both files is
			rejected at load time, so the deployment must choose which matrix is authoritative.
			Embedding, <code>similarity</code>, and <code>mostSimilar</code> behave the same as on
			the float model, up to the quantization error of the chosen bit width; fewer bits
			trade fidelity for size.
		</para>
		<para>
			<code>StaticEmbeddingModelQuantizedTest</code> asserts this workflow end to end:
			quantizing a directory, loading it after the safetensors is removed, and matching the
			float model's embeddings and rankings; <code>QuantizedEmbeddingMatrixTest</code> pins
			the reconstruction quality at each bit width.
		</para>
	</section>

	<section xml:id="tools.embeddings.safetensors">
		<title>The safetensors Reader</title>
		<para>
			Weights are read with a purpose-built reader for the safetensors format. Unlike
			pickle-based checkpoint formats, a safetensors file carries no executable content
			(the header is data-only JSON and the body is raw tensor bytes), so loading one
			cannot execute arbitrary code. Only the header is read eagerly; tensor data streams
			directly into the decoded array, so the file size is not limited by Java's
			int-indexed arrays. One decoded tensor is capped at the maximum Java array length
			(about 2.1 billion float elements), checked explicitly.
		</para>
	</section>
</chapter>
