<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE chapter PUBLIC "-//OASIS//DTD DocBook XML 5.0//EN"
"http://docbook.org/xml/5.0/dtd/docbook.dtd"[
]>
<!--
Licensed to the Apache Software Foundation (ASF) under one
or more contributor license agreements.  See the NOTICE file
distributed with this work for additional information
regarding copyright ownership.  The ASF licenses this file
to you under the Apache License, Version 2.0 (the
"License"); you may not use this file except in compliance
with the License.  You may obtain a copy of the License at

   http://www.apache.org/licenses/LICENSE-2.0

Unless required by applicable law or agreed to in writing,
software distributed under the License is distributed on an
"AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
KIND, either express or implied.  See the License for the
specific language governing permissions and limitations
under the License.
-->

<chapter xml:id="tools.model" xmlns:xlink="http://www.w3.org/1999/xlink">
    <title>Classpath Loading of OpenNLP Models</title>

  <section xml:id="tools.model.background">
    <title>Background</title>
    <para>
      Since version 2.4.0, OpenNLP supports the ability to load pre-trained OpenNLP models from the classpath.
      It relies on either a simple implementation using the application's classpath or on the
      <link xlink:href="https://github.com/classgraph/classgraph">classgraph</link>
      library to locate OpenNLP model JAR files.
      Our pre-trained models are bundled from the
      <link xlink:href="https://github.com/apache/opennlp-models">OpenNLP Models repository</link>.

      This chapter describes

      <itemizedlist>
          <listitem>
              <para>how to load and use a pre-trained OpenNLP model available in the classpath.</para>
          </listitem>
          <listitem>
              <para>how to bundle a custom OpenNLP model to be loadable as a JAR file from the classpath.</para>
          </listitem>
      </itemizedlist>
    </para>
  </section>

	<section xml:id="tools.model.load">
		<title>Loading a pre-trained model</title>
      <para>
        First, you need to add the following dependency to your classpath:

        <programlisting language="xml">
<![CDATA[<dependency>
  <groupId>org.apache.opennlp</groupId>
  <artifactId>opennlp-model-resolver</artifactId>
  <version>CURRENT_OPENNLP_VERSION</version>
</dependency>]]>
        </programlisting>

        by using our pre-trained models or by building custom models as described later in this section.
        If you need advanced classpath scanning capabilities, you can also add the classgraph library to your classpath.

        <programlisting language="xml">
<![CDATA[<dependency>
    <groupId>io.github.classgraph</groupId>
    <artifactId>classgraph</artifactId>
    <version>CURRENT_CLASSGRAPH_VERSION</version>
</dependency>]]>
        </programlisting>

        Make sure you replace the placeholders with the appropriate version values.
        Next, you can search for such a model and load it from the classpath:

        <programlisting language="java">
<![CDATA[final ClassgraphModelFinder finder = new ClassgraphModelFinder(); // or use: new SimpleClassPathModelFinder()
final ClassPathModelLoader loader = new ClassPathModelLoader();
final Set<ClassPathModelEntry> models = finder.findModels(false);
for(ClassPathModelEntry entry : models) {

     final ClassPathModel model = loader.load(entry);
     if(model != null) {
          System.out.println(model.getModelName());
          System.out.println(model.getModelSHA256());
          System.out.println(model.getModelVersion());
          System.out.println(model.getModelLanguage());
          // do something with the model by consuming the byte array
     }
}]]>
        </programlisting>
    </para>
    <para>
      Moreover, certain OpenNLP models can be obtained via a
      <emphasis>ClassPathModelProvider</emphasis>, such as OpenNLP's
      built-in <emphasis>DefaultClassPathModelProvider</emphasis> class.
      It allows direct use of models available under a certain locale, given
      that those are present in the classpath and can be loaded.

      <programlisting language="java">
<![CDATA[final ClassPathModelProvider provider = new DefaultClassPathModelProvider(finder, loader);
// Here: SentenceModel, other model types accordingly
final SentenceModel sm = provider.load("en", opennlp.tools.models.ModelType.SENTENCE_DETECTOR, SentenceModel.class);
if(sm != null) {
    // do something with the (sentence) model
}]]>
      </programlisting>

      In this example, the finder and loader objects can be created or re-used as shown in the previous code example.
      <note>
        <para>
          When running on Java 21+ (the minimum supported version since OpenNLP 3.0.0), the JVM argument
          <screen>
<![CDATA[--add-opens java.base/jdk.internal.loader=ALL-UNNAMED]]>
          </screen>
          may be required. Without this parameter, OpenNLP uses the JVM bootstrap classpath to locate models
          rather than the UCP class loader.
          For more advanced or non-standard class loading scenarios, using ClassGraph and implementing a
          custom provider may cover additional cases beyond the default UCP class loader or
          JVM bootstrap class path behavior.
        </para>
      </note>
    </para>
	</section>

	<section xml:id="tools.model.bundle">
		<title>Bundling a self-trained model</title>
		<para>
      If you intend to provide your own custom trained OpenNLP models as JAR files for classpath discovery,
      we recommend that you have a look at our setup in the <link xlink:href="https://github.com/apache/opennlp-models">OpenNLP Models
      repository</link>. We recommend to bundle one model per JAR file.

      Make sure you add a <emphasis>model.properties</emphasis> file with the following content:

      <programlisting language="java">
<![CDATA[model.name=${model.name}
model.version=${model.version}
model.sha256=${model.sha256}
model.language=${model.language}]]>
      </programlisting>

      Make sure to replace the values accordingly and configure your build tool to include the binary model and the <emphasis>model.properties</emphasis>
      in the resulting JAR file.

      To load such a custom model, you may need to adjust the pattern for classpath scanning. For example, if you name the model "custom-opennlp-model",
      you need the following code to successfully find and load it:

      <programlisting language="java">
<![CDATA[final ClassgraphModelFinder finder = new ClassgraphModelFinder("custom-opennlp-model.jar"); // or use: new SimpleClassPathModelFinder("custom-opennlp-model.jar")
final ClassPathModelLoader loader = new ClassPathModelLoader();
final Set<ClassPathModelEntry> models = finder.findModels(false);
for(ClassPathModelEntry entry : models) {
    final ClassPathModel model = loader.load(entry);
    if(model != null) {
      System.out.println(model.getModelName());
      System.out.println(model.getModelSHA256());
      System.out.println(model.getModelVersion());
      System.out.println(model.getModelLanguage());
      // do something with the model by consuming the byte array
    }
}]]>
      </programlisting>

    </para>
  </section>

  <section xml:id="tools.model.resource-installer">
    <title>Installing third-party resources</title>
    <para>
      <code>ResourceInstaller</code> copies a supplied <code>http</code>,
      <code>https</code>, or <code>file</code> URI into a target directory. Http and https
      sources require a SHA-256 or SHA-512 checksum. File sources may omit it.
      Gzip-compressed tar and zip archives are unpacked, plain gzip files are decompressed,
      and other content is stored under its source name. OpenNLP <code>*.bin</code> model
      files remain packed.
    </para>
    <programlisting language="java">
<![CDATA[Path result = ResourceInstaller.install(
    corpusTarGz.toUri(), targetDirectory, expectedSha256);
// result is targetDirectory; the archived files appear under their archive paths]]>
    </programlisting>
    <para>
      Content is unpacked in a staging directory on the target filesystem. Archive paths
      cannot escape that directory, duplicate file paths are rejected, and existing target
      files are not replaced. A checksum, download, or unpacking error promotes no files.
      Promotion also rejects symbolic links below the target. Concurrent changes to the
      target directory are outside this guarantee.
    </para>
    <para>
      Http and https requests use connection and read timeouts, follow up to five
      redirects by default, and reject an https-to-http downgrade. Default limits are 1 GiB
      downloaded, 4 GiB expanded, and 100000 archive entries; compressed content may
      expand to at most 100 times its compressed size, with a 1 MiB floor for small
      sources. These defaults can be set at
      JVM startup with
      <code>opennlp.download.max.bytes</code>, <code>opennlp.install.max.total.bytes</code>,
      <code>opennlp.install.max.entries</code>, and
      <code>opennlp.install.max.expansion.ratio</code>. Use
      <code>ResourceInstaller.Limits</code> to set limits for one call.
    </para>
    <para>
      The expansion ratio and the expansion limit are enforced independently and the
      tighter one applies, so raising <code>opennlp.install.max.total.bytes</code> alone
      does not admit an archive that exceeds the ratio. A resource that legitimately
      compresses better than 100 to 1, such as a corpus of highly repetitive text, needs
      <code>opennlp.install.max.expansion.ratio</code> raised as well.
    </para>
    <programlisting language="java">
<![CDATA[ResourceInstaller.Limits limits = ResourceInstaller.Limits.builder()
    .readTimeout(Duration.ofSeconds(10))
    .maxDownloadBytes(1024L * 1024)   // download limit in bytes
    .maxExpandedBytes(1024L * 1024)   // expansion limit in bytes
    .maxExpansionRatio(500)           // expanded bytes per compressed byte
    .build();
Path result = ResourceInstaller.install(
    corpusTarGz.toUri(), targetDirectory, expectedSha256, limits);]]>
    </programlisting>
  </section>
</chapter>
