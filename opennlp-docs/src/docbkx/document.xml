<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE chapter PUBLIC "-//OASIS//DTD DocBook XML 5.0//EN"
"http://docbook.org/xml/5.0/dtd/docbook.dtd"[
]>
<!-- Licensed to the Apache Software Foundation (ASF) under one or more contributor
	license agreements. See the NOTICE file distributed with this work for additional
	information regarding copyright ownership. The ASF licenses this file to
	you under the Apache License, Version 2.0 (the "License"); you may not use
	this file except in compliance with the License. You may obtain a copy of
	the License at http://www.apache.org/licenses/LICENSE-2.0 Unless required
	by applicable law or agreed to in writing, software distributed under the
	License is distributed on an "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS
	OF ANY KIND, either express or implied. See the License for the specific
	language governing permissions and limitations under the License. -->

<chapter xml:id="tools.document" xmlns:xlink="http://www.w3.org/1999/xlink">

	<title>Document Annotation Container</title>

	<section xml:id="tools.document.introduction">
		<title>Introduction</title>
		<para>
			The task APIs of OpenNLP each return one kind of result: a tokenizer returns
			spans, a tagger returns tags, a name finder returns names. An application that
			runs several of them over one text has to keep those results aligned itself,
			and structured results such as coreference chains or dependency arcs, which
			refer to other results, have no place to live at all. The document container
			gives them one. It adds a small API next to the task APIs and replaces none of
			them: a task that needs only one component keeps calling it directly.
		</para>
		<para>
			The package <code>opennlp.tools.document</code> provides an immutable container
			that carries the original text of one document together with any number of
			annotation layers over it. A layer is a list of <code>Annotation</code> values,
			each pairing a <code>Span</code> with a value. A span's offsets count Java
			chars (UTF-16 units, so a supplementary-plane character counts as two) from
			the beginning of the text exactly as the caller supplied it, never a
			normalized or derived form, so every annotation can be highlighted in
			the source text. The value's Java type is part of the layer's identity: a token
			layer reads back as <code>List&lt;Annotation&lt;String&gt;&gt;</code>, for
			example the token <code>The</code> paired with the span <code>[0..3)</code>.
		</para>
		<example xml:id="tools.document.introduction.stacked">
			<title>A document with three annotation layers</title>
			<literallayout class="monospaced">
text:      The dog barks. It naps.
           0         1         2
           01234567890123456789012

sentences: [0..14) "The dog barks."   [15..23) "It naps."
tokens:    [0..3) "The" [4..7) "dog" [8..14) "barks." [15..17) "It" [18..23) "naps."
pos-tags:  [0..3) "DT"  [4..7) "NN"  [8..14) "VBZ"    [15..17) "PRP" [18..23) "VBZ"</literallayout>
		</example>
		<para>
			Each layer in the figure above is identified by a <code>LayerKey</code> that
			names the layer and declares the type of its values:
			<code>Layers.TOKENS</code> is a <code>LayerKey&lt;String&gt;</code>, so
			<code>document.get(Layers.TOKENS)</code> returns the token annotations with
			string values. Two keys are equal when their id, their value type, and their
			scope are equal, so independently created constants for the same layer
			interoperate. The keys of the core linguistic layers live in
			<code>Layers</code>; a capability-specific layer's key lives on the annotator
			that provides it, and adding a capability never changes the container.
		</para>
		<para>
			Key ids are namespaced. Keys OpenNLP defines start with the
			<code>opennlp:</code> prefix; a user extension must use its own prefix. A
			bare id, like the <code>token-lengths</code> key below, is legal for an
			application-local layer. Toolkit keys are created through
			<code>Layers.key</code> and <code>Layers.documentKey</code>, which apply the
			prefix.
		</para>
		<para>
			A document is never modified in place: adding a layer returns a new document
			that shares the unchanged layers with its ancestor. Documents created through
			<code>Document.of</code> capture their text at construction and are safe to
			share between threads. Annotations reference other annotations by index within
			their layer, never by object identity. Three invariants make those index
			references sound: a layer keeps its insertion order, a layer is immutable once
			added and detached from the caller's input list, and providing a layer that
			already exists is rejected, so an index reference stays valid for the lifetime
			of the document.
		</para>
		<para>
			A key declares its scope. Keys are positional by default: every annotation
			carries a span, and consumers never null-check it. A document-scoped key
			carries whole-document values without spans, which is where a language id,
			a category distribution, or provenance belongs:
		</para>
		<programlisting language="java"><![CDATA[
LayerKey<String> LANGUAGE = LayerKey.document("app:language", String.class);

Document tagged = document.with(LANGUAGE, List.of(Annotation.of("eng")));
String language = tagged.get(LANGUAGE).get(0).value(); // "eng", span is null]]>
		</programlisting>
		<para>
			A corpus may carry a hand-annotated version of a layer beside a produced one.
			The convention is a <code>gold:</code> id prefix on the same key scheme, for
			example <code>gold:opennlp:tokens</code> beside <code>opennlp:tokens</code>;
			since adding a layer is once-only, competing versions always live under
			distinct keys and never replace each other.
		</para>
		<para>
			Each rule above is pinned by a test in <code>DocumentContractTest</code>:
			<code>testKeysWithSameIdButDifferentTypesAreDifferentLayers</code>,
			<code>testToolkitKeysCarryTheNamespacePrefix</code>,
			<code>testDocumentScopedLayersCarrySpanlessValues</code>,
			<code>testLayerPreservesInsertionOrder</code>,
			<code>testDuplicateLayerIsRejectedWithExactMessage</code>,
			<code>testGoldLayerLivesBesideThePredictedLayer</code>, and
			<code>testCustomLayerNeedsNoContainerChange</code>.
		</para>
		<para>
			None of this is new architecture, and that is intended. Standoff annotations
			over an unchanged original text are how the UIMA CAS works; typed keys that let
			any module add a layer without the container knowing about it are the pattern of
			Stanford CoreNLP's annotation map; and annotators that declare what they require
			and provide, checked when the pipeline is assembled, follow spaCy's pipeline
			analysis. The container takes the lightweight end of each: string ids with a
			namespace instead of class identity, and no type system descriptors.
		</para>
	</section>

	<section xml:id="tools.document.layers">
		<title>What the layers contain</title>
		<para>
			A layer has one value type and one annotation per unit, anchored on the span
			of that unit in the original text. The toolkit's own adapters provide these
			layers:
		</para>
		<table xml:id="tools.document.layers.table">
			<title>Layers provided by the toolkit's adapters</title>
			<tgroup cols="4">
				<thead>
					<row>
						<entry>Key</entry>
						<entry>Value</entry>
						<entry>One annotation per</entry>
						<entry>Provided by</entry>
					</row>
				</thead>
				<tbody>
					<row>
						<entry><code>opennlp:sentences</code></entry>
						<entry><code>String</code>, the sentence text</entry>
						<entry>sentence</entry>
						<entry><code>SentenceDetectorAnnotator</code></entry>
					</row>
					<row>
						<entry><code>opennlp:tokens</code></entry>
						<entry><code>String</code>, the token</entry>
						<entry>token</entry>
						<entry><code>TokenizerAnnotator</code></entry>
					</row>
					<row>
						<entry><code>opennlp:pos</code></entry>
						<entry><code>String</code>, the tag</entry>
						<entry>token, on the token's span</entry>
						<entry><code>POSTaggerAnnotator</code></entry>
					</row>
					<row>
						<entry><code>opennlp:lemmas</code></entry>
						<entry><code>String</code>, the lemma</entry>
						<entry>token, on the token's span</entry>
						<entry><code>LemmatizerAnnotator</code></entry>
					</row>
					<row>
						<entry><code>opennlp:stems</code></entry>
						<entry><code>String</code>, the stem</entry>
						<entry>token, on the token's span</entry>
						<entry><code>StemmerAnnotator</code></entry>
					</row>
					<row>
						<entry><code>opennlp:entities</code></entry>
						<entry><code>String</code>, the entity type</entry>
						<entry>name, on the name's span</entry>
						<entry><code>NameFinderAnnotator</code></entry>
					</row>
					<row>
						<entry><code>opennlp:chunks</code></entry>
						<entry><code>String</code>, the chunk type</entry>
						<entry>chunk</entry>
						<entry><code>ChunkerAnnotator</code></entry>
					</row>
					<row>
						<entry><code>opennlp:phrases</code></entry>
						<entry><code>Phrase</code>: the label and the head span</entry>
						<entry>constituent</entry>
						<entry><code>ParserAnnotator</code></entry>
					</row>
				</tbody>
			</tgroup>
		</table>
		<para>
			The keys of the first four layers in the table live in <code>Layers</code>;
			the others live on the annotator that provides them, which is also where an
			extension puts its key. Layers that refer to other layers do so by index into
			them, which the invariants above keep valid: a coreference layer, for example,
			puts one annotation on each mention's span with the chain it belongs to, and a
			dependency layer puts one annotation on each token's span naming the head token
			by its index in <code>opennlp:tokens</code>. Adding such a layer requires no change
			in this package; <code>testCustomLayerNeedsNoContainerChange</code> pins that,
			and the section below shows how it is done.
		</para>
	</section>

	<section xml:id="tools.document.pipeline">
		<title>Building a pipeline</title>
		<para>
			A pipeline step implements <code>DocumentAnnotator</code>: it reads layers from a
			document and returns a new document with its own layers added. An annotator
			declares the layers it requires and provides, and a
			<code>DocumentAnalyzer</code> validates those declarations when the pipeline is
			assembled: every required layer must be provided by an earlier annotator, and no
			two annotators may provide the same layer, so a misordered pipeline fails at
			build time rather than midway through a document.
		</para>
		<para>
			Adapters for the toolkit's own components are provided:
			<code>SentenceDetectorAnnotator</code>, <code>TokenizerAnnotator</code>,
			<code>POSTaggerAnnotator</code>, <code>NameFinderAnnotator</code>,
			<code>ChunkerAnnotator</code>, <code>ParserAnnotator</code>,
			<code>LemmatizerAnnotator</code>, and <code>StemmerAnnotator</code>. Each wraps
			one of the existing component APIs; an application that needs only one task
			can keep using that component's API directly. The following pipeline combines
			three adapters with one custom annotator and analyzes the text from the
			introduction's figure; the components behind the adapters are any
			<code>SentenceDetector</code>, <code>Tokenizer</code>, and
			<code>POSTagger</code>, for example the ME implementations loaded from models:
		</para>
		<programlisting language="java"><![CDATA[
DocumentAnalyzer analyzer = DocumentAnalyzer.builder()
    .add(new SentenceDetectorAnnotator(sentenceDetector))
    .add(new TokenizerAnnotator(tokenizer))
    .add(new POSTaggerAnnotator(tagger))
    .add(new TokenLengthAnnotator())
    .build();

Document document = analyzer.analyze("The dog barks. It naps.");]]>
		</programlisting>
		<para>
			<code>DocumentPipelineExampleTest</code> asserts the pipeline and layer
			round-trip shown here; <code>DocumentAnalyzerTest</code> covers the build-time
			checks, for example <code>testMisorderedPipelineFailsAtBuildTime</code>.
		</para>
		<para>
			The resulting document carries exactly the four layers the pipeline provides:
			the figure's three plus the custom token-lengths layer. The sentence
			layer holds two annotations, <code>[0..14)</code> covering
			<code>The dog barks.</code> and <code>[15..23)</code> covering
			<code>It naps.</code>. The token layer holds five tokens whose spans refer to
			the document text even inside the second sentence, so
			<code>It</code> is <code>[15..17)</code> and <code>naps.</code> is
			<code>[18..23)</code>. Layers produced per token stay aligned with the token
			layer by position:
		</para>
		<programlisting language="java"><![CDATA[
List<Annotation<String>> tokens = document.get(Layers.TOKENS);
List<Annotation<String>> tags = document.get(Layers.POS_TAGS);
for (int i = 0; i < tags.size(); i++) {
  // each tag sits on its token's span, e.g. "DT" on [0..3) for "The"
  Span span = tags.get(i).span();
}

// every span refers to the original text, so covered text round-trips
for (Annotation<String> token : tokens) {
  CharSequence covered = token.span().getCoveredText(document.text());
}]]>
		</programlisting>
		<para>
			Because a document is immutable, independent pipelines can process the same
			text in parallel, each starting from its own <code>Document.of(text)</code>,
			and their results can be joined afterwards: <code>merge</code> returns a new
			document carrying the layers of both. The texts must match, and by default a
			layer key present on both documents is rejected, so two branches that both
			run a tokenizer collide on <code>opennlp:tokens</code>. When the branches
			rebuild a shared prefix identically, passing
			<code>DuplicateLayerPolicy.KEEP_EQUAL</code> keeps one copy of each agreeing
			layer; copies whose contents differ are still rejected.
		</para>
	</section>

	<section xml:id="tools.document.custom">
		<title>Writing a custom annotator</title>
		<para>
			The annotator below defines its own key in its own code, reads the token
			layer, and provides one integer annotation per token; the analyzer's
			build-time validation guarantees a tokenizer ran earlier:
		</para>
		<programlisting language="java"><![CDATA[
static final LayerKey<Integer> TOKEN_LENGTHS =
    LayerKey.of("token-lengths", Integer.class);

class TokenLengthAnnotator implements DocumentAnnotator {

  @Override
  public Document annotate(Document document) {
    DocumentAnnotators.requireLayers(document, Layers.TOKENS);
    List<Annotation<String>> tokens = document.get(Layers.TOKENS);
    List<Annotation<Integer>> lengths = new ArrayList<>(tokens.size());
    for (Annotation<String> token : tokens) {
      lengths.add(new Annotation<>(token.span(), token.value().length()));
    }
    return document.with(TOKEN_LENGTHS, lengths);
  }

  @Override
  public Set<LayerKey<?>> requires() {
    return Set.of(Layers.TOKENS);
  }

  @Override
  public Set<LayerKey<?>> provides() {
    return Set.of(TOKEN_LENGTHS);
  }
}]]>
		</programlisting>
		<para>
			Because the key declares the value type, the values come back as numbers
			without a cast; for the text above the five values are
			<code>3, 3, 6, 2, 5</code>, each on its token's span:
		</para>
		<programlisting language="java"><![CDATA[
List<Annotation<Integer>> lengths = document.get(TOKEN_LENGTHS);
int firstTokenLength = lengths.get(0).value(); // 3, for "The" at [0..3)]]>
		</programlisting>
		<para>
			Required layers must be present, but they may be empty: an empty token layer
			yields the annotator's provided layers present but empty, so a pipeline degrades
			gracefully on documents without content. An absent required layer is rejected
			with an <code>IllegalArgumentException</code> naming the layer, because a
			missing pipeline stage is an assembly error, not an empty document.
			<code>DocumentAnnotators.requireLayers</code> performs exactly that rejection,
			including the null check on the document, and is what the toolkit's own
			adapters use.
		</para>
	</section>

	<section xml:id="tools.document.termvectors">
		<title>Term vectors</title>
		<para>
			<code>TermVectorAnnotator</code> rolls the token layer up into a document-scoped
			term vector layer for index consumers: one <code>TermVector</code> per distinct
			term, carrying the term string, its occurrence count, and the occurrence spans in
			original text coordinates. Term identity comes from the annotator's inputs: without
			a normalizer the term is the token's covered text as-is; with a plain
			<code>CharSequenceNormalizer</code>, the general path, each token's covered text is
			normalized on its own to become the term; with an
			<code>OffsetAwareNormalizer</code> the document text is normalized once and each
			token's covered normalized text becomes the term. On every path tokens differing
			only by a normalization fold group together while their spans keep pointing into
			the original text. A token whose normalized form is empty is omitted from the
			layer. The layer preserves first-occurrence order.
			<code>TermVectorPipelineTest#testTokenizerAndTermVectorPipeline</code> asserts the
			behavior shown here.
			<programlisting language="java"><![CDATA[
DocumentAnalyzer analyzer = DocumentAnalyzer.builder()
    .add(new TokenizerAnnotator(tokenizer))
    .add(new TermVectorAnnotator())
    .build();

Document document = analyzer.analyze("The dog barks. The dog naps.");
List<Annotation<TermVector>> vectors = document.get(TermVectorAnnotator.TERM_VECTORS);
// vectors.get(1).value() is ("dog", 2, [[4..7), [19..22)])]]>
			</programlisting>
		</para>
		<para>
			The normalized-term workflow passes any shipped
			<code>CharSequenceNormalizer</code> to the plain-normalizer constructor: the
			normalizer is applied to each token's covered text to produce the term, so the
			case, NFC, and accent folds that <code>buildAligned()</code> rejects all work
			here, and every occurrence span stays the token's own span in the original text.
			<code>TermVectorNormalizedExampleTest#testCaseFoldedTermsKeepOriginalSpans</code>
			asserts the behavior shown here.
			<programlisting language="java"><![CDATA[
CharSequenceNormalizer folder = TextNormalizer.builder().caseFold().build();
DocumentAnalyzer analyzer = DocumentAnalyzer.builder()
    .add(new TokenizerAnnotator(WhitespaceTokenizer.INSTANCE))
    .add(new TermVectorAnnotator(folder))
    .build();

Document document = analyzer.analyze("Word word WORD");
List<Annotation<TermVector>> vectors = document.get(TermVectorAnnotator.TERM_VECTORS);
// vectors.get(0).value() is ("word", 3, [[0..4), [5..9), [10..14)])]]>
			</programlisting>
		</para>
		<para>
			Scoring-only consumers that never read offsets can skip storing them:
			<code>new TermVectorAnnotator(TermVectorAnnotator.Mode.SCORING_ONLY)</code> emits
			vectors that carry counts but no spans, and
			<code>TermVector.count("dog", 2)</code> builds the matching expectation in tests.
		</para>
	</section>
</chapter>
